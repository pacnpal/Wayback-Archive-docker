services:
  wayback-archive:
    build: .
    image: wayback-archive:local
    container_name: wayback-archive
    environment:
      OUTPUT_DIR: /app/output
    ports:
      - "8765:8765"
    volumes:
      - ./output:/app/output
    restart: unless-stopped
